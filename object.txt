fn add (x, y) {
  let result = x + y;
  print(result)

  result
}

fn sub () {
  fn add (x, y) {
    let result = x + y;
    print(result)
    
    result
  }

  let foo = 45; 
}

const result = add(10,4);

print(result)